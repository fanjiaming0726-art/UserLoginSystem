server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo_auth?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    # ????????????
    map-underscore-to-camel-case: true
app:
  jwt:
    secret: "please_change_this_to_a_long_random_string_32_chars_min"
    expire-seconds: 604800   # 7?